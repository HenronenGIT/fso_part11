name: Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploymet:
    runs-on: ubuntu-20.04
    steps:
        - uses: actions/checkout@v3
        - uses: actions/setup-node@v3
        - name: Install dependencies
          run: npm install
